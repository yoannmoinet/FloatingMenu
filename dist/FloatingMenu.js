(function(root, undefined) {

  "use strict";


/* FloatingMenu main */

// Base function.
var FloatingMenu = function() {
  // Add functionality here.
  return true;
};


// Version.
FloatingMenu.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.FloatingMenu = FloatingMenu;


}(this));
